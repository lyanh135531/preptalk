{
  "version": 2,
  "buildCommand": "cd ../.. && npm install && npm run build -w @preptalk/web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
